dependencies {
    compile project(':parser')
}

task run(type: JavaExec, dependsOn: classes) {
    main = 'org.mc.mcc.Main'
    classpath sourceSets.main.runtimeClasspath
    classpath configurations.runtime
}